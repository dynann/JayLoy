{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "nixpacks",
    "buildCommand": "npm install && npm install framer-motion@12.5.0 && npm run build",
    "environment": {
      "NODE_VERSION": "20",
      "NPM_CONFIG_LOGLEVEL": "verbose"
    }
  },
  "deploy": {
    "startCommand": "npm run start",
    "healthcheckPath": "/",
    "healthcheckTimeout": 180,
    "restartPolicyType": "always"
  },
  "postdeploy": {
    "script": "echo 'Dependency check complete. Verify packages with npm list'"
  }
}
